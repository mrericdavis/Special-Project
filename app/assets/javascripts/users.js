/* global $ */
$(document).ready(function() {
  // $('.carousel').carousel({dist: -100, indicators: true, shift:10, padding: 100, fullWidth: false});
  // $('.carousel').carousel({dist: -100, indicators: true, shift:10, padding: 100, fullWidth: true});
  $('.carousel').carousel();
});

// $('.carousel.carousel-slider').carousel({fullWidth: true, shift: 0, padding: 10});


// // Next slide
// $('.carousel').carousel('next');
// $('.carousel').carousel('next', 3); // Move next n times.

// // Previous slide
// $('.carousel').carousel('prev');
// $('.carousel').carousel('prev', 4); // Move prev n times.

// // Set to nth slide
// $('.carousel').carousel('set', 4);

// // Destroy carousel
// $('.carousel').carousel('destroy');